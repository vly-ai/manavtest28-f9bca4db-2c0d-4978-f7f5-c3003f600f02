"use client"

import { api } from "@/convex/_generated/api"; // import using @/convex/_generated/api


/**
 * Dashboard page. Add functionality here. The landing page buttons for getting started lead to this page.
 * @returns 
 */
export default function Dashboard() {

  return (
    <div className="h-full p-4 w-full">
      <h2 className="text-xl font-semibold mb-1">This is the dashboard, courtesy of vly.ai (YC F24). Add functionality here.</h2>
    </div>
  );
}
